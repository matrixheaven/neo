# Minimal Neo config example for ~/.neo/config.toml (or $NEO_HOME/config.toml).

default_provider = "fake"
default_model = "fake"
sessions_dir = "~/neo-sessions"
permission_mode = "ask"

[defaults]
mode = "interactive"

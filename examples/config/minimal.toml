# Minimal Neo project config example for .neo/config.toml.

default_provider = "fake"
default_model = "fake"
sessions_dir = ".neo/sessions"

[permissions]
file_read = "Allow"
file_write = "Ask"
shell = "Ask"

[defaults]
mode = "interactive"
